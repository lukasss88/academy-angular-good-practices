// avoid

function Dashboard($location, $routeParams, common, dataservice)
{
}

angular.module('app').controller('DashboardController', ['$location', '$routeParams', 'common', 'dataservice', Dashboard]);
